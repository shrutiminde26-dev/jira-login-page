name: CI Pipeline

on:
  push:
    branches:
      - SCRUM-5-login-page
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify Project Files
        run: |
          echo "GitHub Actions CI Pipeline Running Successfully"
          ls -la